一，对string类型的进行全枚举
for (int i = 0; i <= n - k; ++i) {
            // 枚举所有长度为 k 的子串
            int tmp = stoi(s.substr(i, k));
            if (tmp && num % tmp == 0) {
                ++res;
            }
        }
   但是这个是固定长度（k）。
   substr(a,b),是从a开始，长度为b的字符串
 二，1903. 字符串中的最大奇数
 1，最大奇数就是，最长的
 2，所谓奇数，就是看最后一位数字是不是奇数。
   
