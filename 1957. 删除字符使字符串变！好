15-21
1,不会啊，总不对
2，不过确实要把容器的函数都积累一下。

        for(int i=0;i<s.size()-2;i++)
        {
            if(s[i]==s[i+1]&&s[i+1]==s[i+2])
            {
                s.erase(i+2);
                
                
            }
        }
        return s;
  为啥不行
